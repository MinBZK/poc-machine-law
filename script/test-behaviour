#!/usr/bin/env sh
set -e

# Run behavior tests with Behave
# Pass all arguments to behave (e.g., --tags="~@ui")
# Exclude features/pending/ directory (work-in-progress features)
uv run behave features --exclude="pending/" "$@"
