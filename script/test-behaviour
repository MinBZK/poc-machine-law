#!/usr/bin/env sh
set -e

# Run behavior tests with Behave
# Pass all arguments to behave (e.g., --tags="@zorgtoeslag")
# Skip @ui tests by default (require Playwright browser installation)
uv run behave features --tags="~@ui" "$@"
